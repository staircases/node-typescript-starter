export interface QueryHandlingAttributes {
    qid: string
    subDate: Date
    custId: string
    empId: string
    resDate: Date
    status: string
    feedback: number
    queryText: string
    queryReponse: string
}
